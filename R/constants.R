SPECIAL_LHS <- c(
  "initial", "deriv", "update", "output", "dim", "config", "compare")
TIME <- "t"
DT <- "dt"

COMPARE <- list(
  Normal = function(mean, sd) {},
  Poisson = function(lambda) {})

FUNCTIONS <- list(
  exp = function(x) {})
